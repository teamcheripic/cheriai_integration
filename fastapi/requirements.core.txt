# =============================================================================
# CheriPic FastAPI — CORE requirements (no ML, cPanel-safe)
# =============================================================================
# This is the subset that installs on GoDaddy cPanel / shared Python hosting:
# pure-Python wheels + cryptography (has cp311 manylinux wheels on PyPI).
#
# Everything in here is what /chat, /billing/*, /admin/*, conversation history,
# and merchant lookups need. KYC face match (insightface + onnxruntime +
# opencv + numpy) is in requirements.face.txt and is OPTIONAL — without it the
# /kyc/verify-face endpoint returns a clean 503, every other route works.
#
# Install on cPanel:
#   1. Setup Python App → choose Python 3.11
#   2. Set "requirements.txt" to this filename (or rename it locally)
#   3. Restart the app from the cPanel UI
# =============================================================================

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.11.0
certifi==2025.11.12
cffi==2.0.0
click==8.3.1
cryptography==46.0.3
deprecation==2.1.0
distro==1.9.0
fastapi==0.121.3
h11==0.16.0
h2==4.3.0
hpack==4.1.0
httpcore==1.0.9
httpx==0.28.1
hyperframe==6.1.0
idna==3.11
jiter==0.12.0
multidict==6.7.0
openai==2.8.1
packaging==25.0
postgrest==2.24.0
propcache==0.4.1
pycparser==2.23
pydantic==2.12.4
pydantic_core==2.41.5
PyJWT==2.10.1
python-dotenv==1.2.1
realtime==2.24.0
slowapi==0.1.9
sniffio==1.3.1
starlette==0.50.0
storage3==2.24.0
StrEnum==0.4.15
supabase==2.24.0
supabase-auth==2.24.0
supabase-functions==2.24.0
tqdm==4.67.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.38.0
websockets==15.0.1
yarl==1.22.0

# Stripe is pure-Python — installs on every host with no compilation.
stripe>=11.0

# WSGI↔ASGI bridge — REQUIRED on GoDaddy/cPanel because Phusion Passenger
# only speaks WSGI and FastAPI is ASGI. Pure-Python, ~30 KB, no compile.
# Used by passenger_wsgi.py to wrap `app` from main.py. Harmless on Render
# /VPS deploys where uvicorn talks to the ASGI app directly.
a2wsgi>=1.10
